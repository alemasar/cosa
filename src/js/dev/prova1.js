import {Methods} from './prova1/prova';
/*
const module = new Proxy(Methods, {
    get: (Target, name, receiver) => {
        const t = new Target();

        return t[name];
    }
});

document.addEventListener('DOMContentLoaded', (e) => {
    console.log('Se ha ejecutado desde prova 1');
    console.log(module.yoqueser(1, 2))
    console.log(module.bar(2, 1))
});
*/

/*
 *document.addEventListener('DOMContentLoaded', (e) => {
 *    console.log('Se ha ejecutado desde prova 1');
 *   const div=document.createElement('DIV');
 *   div.innerHTML='Se ha ejecutado desde prova 1';
 *   document.body.appendChild(div);
 *});
 */
